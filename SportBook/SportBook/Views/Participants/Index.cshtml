@model IEnumerable<SportBook.Models.Participant>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.FkEventNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FkTeamNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FkUserNavigation)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FkEventNavigation.EventId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkTeamNavigation.TeamId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FkUserNavigation.UserId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ParticipantId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ParticipantId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ParticipantId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
